{
  "name": "budget-openrouter",
  "publisher": "alepiumetti",
  "displayName": "budget-openrouter",
  "description": "Para ver cuandos creditos me quedan en open router",
  "icon": "images/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/alepiumetti/budget-openrouter.git"
  },
  "version": "0.0.5",
  "engines": {
    "vscode": "^1.116.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:budget-openrouter.refresh",
    "onCommand:budget-openrouter.showTopModels"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "budget-openrouter.refresh",
        "title": "OpenRouter: Refresh Budget"
      },
      {
        "command": "budget-openrouter.showTopModels",
        "title": "OpenRouter: Show Top Coding Models"
      }
    ],
    "configuration": {
      "title": "Budget OpenRouter",
      "properties": {
        "budgetOpenrouter.apiKey": {
          "type": "string",
          "default": "",
          "markdownDescription": "OpenRouter API key usada para consultar uso y créditos restantes. Alternativa: variable de entorno OPENROUTER_API_KEY.",
          "scope": "application"
        }
      }
    }
  },
  "scripts": {
    "prepare": "sh scripts/setup-git-hooks.sh",
    "bump:from-commits": "sh scripts/auto-bump-from-commits.sh",
    "pre-commit": "npm run bump:from-commits",
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.116.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "eslint": "^9.39.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
